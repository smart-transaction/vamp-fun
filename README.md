# vamp-fun

NOTE! To make deployment working on VMs make sure that you run:
```
gcloud auth configure-docker us-central1-docker.pkg.dev
``` 

That will create a configuration file .docker/config.json.
